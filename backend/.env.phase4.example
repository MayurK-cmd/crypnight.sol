# Phase 4 — Duel Mode Additional Variables
# Add these to your existing .env after deploying the duel contract

# Duel program (deployed in step 1)
DUEL_PROGRAM_ID=BuXUw7XpHNJW8eqwvRiiaepSs8RAuLDVBFPfwFFiXNxn

# Duel treasury PDA (initialized in step 2)
DUEL_TREASURY_PUBKEY=<from init-duel-treasury.ts output>

# Socket.io will use existing variables:
# - FRONTEND_URL (CORS origin for WebSocket connections)
# - PORT (listening socket)
# - JWT_SECRET (for socket auth — already in use for REST auth)
